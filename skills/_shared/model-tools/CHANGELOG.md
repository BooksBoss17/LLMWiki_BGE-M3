# 共享模型与工具变更记录

## 2026-07-13

- 按本机公式冒烟基准与用户决定，将 Pix2Text 1.1.6 标为已安装但禁用：不再参与文字、公式、整页文档的默认候选链；保留权重、哈希、官方来源与历史结果，供日后复测。
- 默认公式识别链调整为 PP-FormulaNet_plus-L 主模型加 TexTeller 交叉证据；二者冲突或命中公式风险标记时仍进入视觉复核，不自动接受。
- 从 `_shared/model-runtime` 迁入完整 OCR、VLM、公式识别和 STT 运行库。
- 从 `_shared/runtime` 迁入 OfficeCLI；旧 1.0.133 主程序与更新包通过迁移哈希校验，首次运行后按实际版本固定为 1.0.135 并重新登记当前 SHA-256。
- 注册表统一命名为 `registry.yaml`，保留质量优先的 `profiles.yaml`。
- Windows 迁移验证增加超长路径支持，模型库 160453 个文件完成全量 SHA-256 等价校验。
